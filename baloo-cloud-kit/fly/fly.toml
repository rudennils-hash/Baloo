# Fly.io app config – Baloo API
app = "baloo-api"
primary_region = "lhr"

[build]
  dockerfile = "../extension/Dockerfile"

[env]
  NODE_ENV = "production"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
